[tox]
envlist = py33, py34

[testenv]
deps = -r{toxinidir}/requirements/develop.txt
usedevelop = True
commands =
    py.test --cov {toxinidir} --cov-report xml -x --cov-config {toxinidir}/.coveragerc {toxinidir}/tests/unit
    coverage run --rcfile={toxinidir}/.coveragerc {envbindir}/behave --stop {toxinidir}/tests/behave/features
